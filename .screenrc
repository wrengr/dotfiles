# ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
# This is wren gayle romano's GNU Screen config     ~ 2016.11.15
# ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~

# ~~~~~ Basic Config
defmonitor on            # monitor activity on all windows
autodetach on            # autodetach screen on hangup (saves running programs)
shell $SHELL             # use my default shell (-$SHELL is a login shell)
defscrollback 1500

# ~~~~~ Display Config
activity "Activity on %" # show "Activity on <windownumber>"
nethack on               # show "nethack-style" screen messages
defutf8 on               # Enable UTF-8

# Always show this caption string as footer.
# 1) show window number, 2) title if it exists
# show 3) system load 4) 'hardstatus of window' if available
# show 5) HH:MM 6) DAY date MONTH
#               (1) (2)   (3)       (4)     (5)  (6)
caption always "%3n %t%? %H: %l  %h%?     %0c  %D %d %M"

# Enable 256-colors (HT: <http://robotsrule.us/vim/>)
attrcolor b ".I"
termcapinfo xterm 'Co#256:AB=\E[48;5;%dm:AF=\E38;5;%dm'
defbce "on"

# ~~~~~ Keybindings
