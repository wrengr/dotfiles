# Unified "header" for: runaway, ra_printlog        ~ 2006.02.22

# Caller must set $_home as necessary before sourcing
# Just in case they didn't set $_home (boo!)
# and are using bash for sh (cuz real sh doesn't do tilde expansion)
[ -z "$_home" ] && _home="~"

# Get general scripting header
. "$_home/local/lib/sh_common"

# set file variables
log="$_files/ralog"
noping="$_files/ranoping"
nossh="$_files/ranossh"
